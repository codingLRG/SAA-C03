Managed service that uses [[GraphQL]]
Different types of ==schemas==:

| Query     | Mutation   | Subscription     |
| --------- | ---------- | ---------------- |
| Read Data | Write Data | Up/Download Data |
Only fetches data that you want, not entire dataset, with a single API call
Resolver populates the data in your schema